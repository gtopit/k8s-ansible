{
  "signing": {
    "default": {
      "expiry": "{{ validity_period }}"
    },
    "profiles": {
      "www": {
         "expiry": "{{ validity_period }}",
         "usages": [
            "signing",
            "key encipherment",
            "server auth",
            "client auth"
        ]
      }
    }
  }
}
